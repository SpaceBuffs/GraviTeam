github - initializing the repo
  996  git init
  997  git add README.md
  998  git commit -a -m "first commit"
  999  git remote add origin https://github.com/SpaceBuffs/GraviTeam.git
 1000  git push -u origin master

github - push to the repo
 1002  git add *.*
 1003  git commit -a -m "second commit"
 1004  git push -u origin master
 1005  meteor deploy sierraflynn.meteor.com

github - pull from the repo
git pull

github - merge
git add *
git commit -a -m ""
git push

meteor - add a database element
meteor add accounts-ui accounts-password
